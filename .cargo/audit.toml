[advisories]
# SQLx records its optional MySQL backend in Cargo.lock. This application enables
# only SQLite, so the vulnerable RSA implementation is not compiled or reachable.
# The advisory has no fixed release; keep this exception narrow and documented.
ignore = ["RUSTSEC-2023-0071"]
