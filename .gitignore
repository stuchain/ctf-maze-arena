# Commit plan and phase docs (implementation guide — keep local)
docs/commit/

# Local SQLite database (created at runtime; see DATABASE_URL in README)
data/*.db

# Local environment files; commit only documented examples
.env
.env.*
!.env.example
!**/.env.example

# Added by cargo

/target
